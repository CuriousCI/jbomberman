name: Publish Docs

on:
  push: 
    branches: 
      - "main"
      
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Javadoc
        run: |
          # sudo apt remove openjdk* --purge
          sudo apt install openjdk-17-jdk
          # sudo apt install openjdk-17-jre
          sudo apt install openjdk-17-doc
          # export JAVA_HOME=/usr/lib/jvm/jdk-17/
        # run: sudo apt install openjdk-jdk
        # run: sudo snap install openjdk

      - name: Build Docs
        run: javadoc -source 17 -d docs -sourcepath src -subpackages jbomberman

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v3

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./docs
          
  deploy:
      environment:
        name: github-pages
        url: ${{ steps.deployment.outputs.page_url }}
      runs-on: ubuntu-latest
      needs: build
      steps:
        - name: Deploy to GitHub Pages
          id: deployment
          uses: actions/deploy-pages@v2
